# Mass IT Solutions Packages

This directory contains all the micro-frontend packages for the Mass IT Solutions web application.

Each package is an isolated module responsible for a specific domain (e.g., `inventory`, `invoices`, `dashboard`), combined together by the `host` package.
